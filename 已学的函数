1，字符串
  a="123456"
  a[1:20]= "23456"
2,find  
  <string>.find(<string1>)  返回字符串string1第一次出现的位置，找不到返回-1
  <string>.find(<string1>， pos)  返回pos位置开始string1第一次出现的位置 
3，append  改变原来的list
  <list>.append(<element>)
  <list> + <list>  不改变原来的lsit
4，len()
   len(<list>)
5,index
  <list>.index(<value>)  返回<value>在list的第一个位置，如果没有返回error
  
6.pop
  <list>.pop()  移除list的最后一个元素并返回
7.split   
  <string>.split()  以空格为分隔符分割string成一个list
  
  8.chr() 将一个字符转换为对应的 assci
  
  9.ord() 将assci码转换为对应的字符

  10. time.sleep()  
      time.ctime()
  11.webbrowser.open(url)
  
  12. save_path = os.getcwd() 获取当前工作目录
  
      os.chdir(r"c:/...")  变换工作目录
      
      os.listdir(r"c://..") 列出指定目录下的所有文件名
      
      os.rename(old_name, new_name) 变换文件名称
      
      string.translate(NULL, "0123456789") 去除string中的数字
      
      
#!/usr/bin/python
 
from string import maketrans   # Required to call maketrans function.
 
intab = "aeiou"
outtab = "12345"
trantab = maketrans(intab, outtab)
 
str = "this is string example....wow!!!";
print str.translate(trantab, 'xm');

from string import maketrans   # 引用 maketrans 函数。
 
intab = "aeiou"
outtab = "12345"
trantab = maketrans(intab, outtab)
 
str = "this is string example....wow!!!";
print str.translate(trantab);
